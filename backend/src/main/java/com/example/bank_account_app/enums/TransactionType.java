package com.example.bank_account_app.enums;

/**
 * Enum to represent the type of transaction.
 */
public enum TransactionType {
    DEBIT, // when money is withdrawn
    CREDIT // when money is deposited
}
